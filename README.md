This is the finddash project.

# Folder structure

├── src
│   └── all the source code for our application. Dash,math etc
│
├── .gitignore
│   └── A gitignore file specifies intentionally untracked files that Git should ignore.


├── docker-compose.yml
└── README.md